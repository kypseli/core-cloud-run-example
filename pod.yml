kind: Pod
metadata:
  name: gitops-pod
spec:
  containers:
  - name: gcp-sdk
    image: google/cloud-sdk:252.0.0-slim
    command:
    - cat
    tty: true
    env:
      - name: GCP_PROJECT
        value: "core-workshop"
  securityContext:
    runAsUser: 1000
